interface:
  display_name: "777codes 图片生成"
  short_description: "通过 777codes MCP 生成、编辑并显示图片"
  default_prompt: "使用 $777codes-image 生成一张图片，并把生成文件显示在当前对话中。"

dependencies:
  tools:
    - type: "mcp"
      value: "777codes-image"
      description: "本地 777codes 图片生成 stdio MCP 服务"
      transport: "stdio"
